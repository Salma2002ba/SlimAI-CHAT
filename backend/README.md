## SlimAI Backend (placeholder)

This directory is reserved for the future SlimAI backend (e.g. Python/FastAPI).

Suggested next steps (later):

- Initialize a Python project (e.g. `pyproject.toml` with FastAPI + uvicorn).
- Add a dedicated Dockerfile for the backend service.
- Expose a clean API that the `frontend/` can consume.

